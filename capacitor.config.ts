import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.leehj825.stickmanr3f',
  appName: 'StickmanAnimator',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
